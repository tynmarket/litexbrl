dependencies:
  pre:
    - AWS_DEFAULT_REGION=ap-northeast-1 aws ec2 authorize-security-group-ingress --group-id sg-f8da2d9e --protocol tcp --port 80 --cidr `curl -s ifconfig.me`/32
machine:
  ruby:
    version: 2.2.2